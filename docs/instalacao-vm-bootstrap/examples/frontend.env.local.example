# Exemplo FICTÍCIO de frontend/.env.local
# Se o frontend roda na mesma VM do backend:
NEXT_PUBLIC_PY_API_URL=http://127.0.0.1:8002

# Se você acessa o frontend de fora (host) e o backend está na VM,
# use o IP da VM (bridged) ou a URL do reverse proxy:
# NEXT_PUBLIC_PY_API_URL=http://192.168.1.50:8002

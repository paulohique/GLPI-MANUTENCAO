# Exemplo FICTÍCIO de python-api/.env
# NÃO cole tokens reais aqui em repositório.

# Database (XAMPP/MariaDB)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=glpi_user
DB_PASSWORD=UmaSenhaForte_Aqui_123
DB_NAME=glpi_manutencao

# GLPI API (somente leitura - GET)
GLPI_BASE_URL=http://glpi.seudominio.local/glpi/apirest.php
GLPI_APP_TOKEN=COLE_SEU_APP_TOKEN_AQUI
GLPI_USER_TOKEN=COLE_SEU_USER_TOKEN_AQUI

# App
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
MAINTENANCE_INTERVAL_DAYS=365

# Auth (LDAP/AD + JWT)
AUTH_ENABLED=true
JWT_SECRET=troque_por_um_segredo_grande_e_unico
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=720

# LDAP (opcional)
LDAP_SERVER=ldaps://dc01.seudominio.local
LDAP_BASE_DN=DC=seudominio,DC=local
LDAP_DOMAIN=seudominio.local
LDAP_USE_SSL=true
LDAP_CONNECT_TIMEOUT_SECONDS=10
